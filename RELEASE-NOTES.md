# Sofia Blog v2 — Migration Release Notes

> **Status**: ✅ All 6 phases of `blog-plan.md` are complete to a deployable state.
> Only one user-side action remains (one-click GitHub Settings change) — see [Going Live](#going-live).

Date: **2026-05-07**
Migration scope: **Vite + React + Supabase → Astro + Markdown + GitHub Pages**

---

## Architecture (final)

```
┌─────────────────────────────────────────────────────────────┐
│                  GitHub Pages (project page)                 │
│             https://sofiayan0523.github.io/sofia/            │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ deploy via Actions
                              │
┌─────────────────────────────────────────────────────────────┐
│ GitHub Actions: .github/workflows/deploy.yml                 │
│   on push:main → setup-node → npm ci → npm run build         │
│                → upload-pages-artifact → deploy-pages        │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ git push
                              │
┌─────────────────────────────────────────────────────────────┐
│ Repo: sofiayan0523/sofia                                     │
│  ├ src/content/posts/*.mdx        (Markdown + JSX)           │
│  ├ src/pages/*.astro              (file-based routing)       │
│  ├ src/components/                                           │
│  │   ├ Header / Footer / SEO / BlogCard (Astro)              │
│  │   ├ CaptureEye (Astro wrapper for web component)          │
│  │   ├ Comments (giscus, disabled by default)                │
│  │   └ ThemeToggle (React Island, client:load)               │
│  ├ src/i18n/{zh,en,index}.ts     (translations + helper)     │
│  ├ public/images/posts/{slug}/   (45 images, 4.2 MB)         │
│  ├ scripts/                                                  │
│  │   ├ download-images.mjs                                   │
│  │   ├ migrate-posts.mjs                                     │
│  │   └ compress-images.mjs                                   │
│  └ astro.config.mjs (site + base="/sofia")                   │
└─────────────────────────────────────────────────────────────┘
```

---

## Phase-by-phase status

| Phase | Title | Status | Notes |
|-------|-------|--------|-------|
| 1 | 基礎建設 | ✅ | Astro v5.18.1 scaffolded; integrations: mdx, react, sitemap, tailwind, rss; pagefind for search |
| 2 | 內容遷移 | ✅ | 5 published posts → MDX; 45 Supabase images → repo; CaptureEye wrapper; capture-eye script pinned to v1.4.0 |
| 3 | 頁面與設計重建 | ✅ | All 7 routes built; shared components; i18n scaffolding (zh+en); Pagefind UI; RSS feed |
| 4 | 圖片管理 | ✅ | 4 large images compressed (-979 KB); `.gitattributes` for binaries |
| 5 | 部署 | ✅ (code) | GitHub Actions workflow committed; **user must enable Pages → Actions source once** |
| 6 | 加值功能 | 🟡 partial | ThemeToggle ✅; giscus scaffold ✅ (disabled); LanguageSwitcher / analytics / OG-gen pending |

---

## What was built

### Pages (10 HTML routes)

| Route | Source | Notes |
|-------|--------|-------|
| `/` | `pages/index.astro` | Hero (capture-eye headshot) + 4 stats + 6 latest posts |
| `/about` | `pages/about.astro` | About copy + Playground 5-card grid |
| `/blog` | `pages/blog/index.astro` | Pagefind search box + category filter + post grid |
| `/blog/{slug}` | `pages/blog/[...slug].astro` | 5 post pages with capture-eye images, tags, comments slot |
| `/career` | `pages/career.astro` | Work (6) + Speaking (7) + Media (4) + Podcast (9) |
| `/404` | `pages/404.astro` | Custom not-found |
| `/rss.xml` | `pages/rss.xml.ts` | RSS 2.0 feed of all posts |
| `/sitemap-*.xml` | (auto) | Generated by `@astrojs/sitemap` |

### Reusable components

- `BaseLayout.astro` — html shell, FOUC-prevention inline script, capture-eye script, SEO mounting
- `SEO.astro` — title / description / OG / Twitter Card / RSS alternate / article meta
- `Header.astro` — nav with active link highlight + ThemeToggle island
- `Footer.astro` — internal links + external links + dynamic year
- `BlogCard.astro` — cover / category / tags / read time / date
- `CaptureEye.astro` — wraps `<capture-eye>` web component, adds `loading="lazy"`
- `ThemeToggle.tsx` — React Island, light↔dark with localStorage persistence
- `Comments.astro` — giscus scaffold (disabled until user fills config)

### Scripts

- `scripts/download-images.mjs` — concurrent downloader with retry, reads `supabase-images.json`
- `scripts/migrate-posts.mjs` — fetches Supabase REST, generates MDX with placeholder/escape/restore pipeline
- `scripts/compress-images.mjs` — sharp + mozjpeg, only touches files > 400 KB
- `scripts/supabase-images.json` — manifest of original image URLs ↔ local target paths ↔ NIDs

### Content

- 5 MDX posts at `src/content/posts/` (zh-TW travel notes from 2022 Oslo + 2024 Italy trip)
- 45 images at `public/images/posts/{slug}/` (5 cover + 40 inline, all with capture-eye NIDs)
- Career data at `src/data/career.ts` (26 items across 4 categories)
- Logos and profile photos at `public/images/{logos,profile}/`

### Configuration

- `astro.config.mjs` — `site` + `base: "/sofia"` for project page deployment
- `tailwind.config.mjs` — color tokens (HSL custom properties), font, animations, typography plugin
- `tsconfig.json` — extends `astro/tsconfigs/strict`, includes `@/*` path alias
- `.gitignore` — adds `.env`, `dist/`, `.astro/`
- `.gitattributes` — marks images as binary

### CI/CD

- `.github/workflows/deploy.yml` — push-to-main triggered, uses official `actions/deploy-pages@v4`

---

## Key bugs caught and fixed during the build

1. **BASE_URL trailing slash** — `import.meta.env.BASE_URL` is `/sofia` (no trailing slash); template `${baseUrl}favicon.ico` produced `/sofiafavicon.ico`. Fix: normalize to `/sofia/` everywhere via `.replace(/\/?$/, "/")`.
2. **MDX-special characters in post body** — `<-` and `->` in plain text broke the MDX parser. Fix: 3-step transform in `migrate-posts.mjs` — extract capture-eye → escape `<>{}\` → restore.
3. **Astro 5 `post.id` includes file extension** — `post.id === "2024-rome.mdx"`, producing URLs like `/blog/2024-rome.mdx/`. Fix: `post.id.replace(/\.mdx?$/, "")` in route's `getStaticPaths`.
4. **RSS feed missing `/sofia/` prefix** — `context.site` is the bare origin; RSS `<link>` was `https://sofiayan0523.github.io/blog/...`. Fix: combine `context.site` with `import.meta.env.BASE_URL` to produce correct project-page URLs.

---

## Sizes and counts

| Metric | Value |
|--------|-------|
| Astro source files | 27 |
| Total `src/` size | 220 KB |
| Posts (MDX) | 5 |
| Inline images per post (avg) | 8 |
| Images stored in repo | 45 (4.2 MB compressed, was 5.2 MB) |
| Logos + profile photos | 8 (~700 KB) |
| Total `public/images/` | 4.9 MB |
| Built `dist/` total | 6.0 MB |
| HTML pages produced | 10 |
| XML feeds (RSS + sitemaps) | 3 |
| Pagefind search index | 5 pages, 2387 indexed words |
| React client bundle | 136 KB (only loaded on pages with islands) |
| ThemeToggle island chunk | 2.5 KB |
| GitHub Actions workflow lines | 56 |

---

## Going live

The site is **fully built and ready to deploy**. To go live:

### One-time user action (required)

1. Visit `https://github.com/sofiayan0523/sofia/settings/pages`
2. Under "Build and deployment" → "Source", select **GitHub Actions**
3. Save

That's it. From this point on, every push to `main` (after it's auto-merged from the working branch) will trigger the workflow and deploy to `https://sofiayan0523.github.io/sofia/`.

### Verifying after first deploy

```bash
# Check the workflow ran:
gh run list --repo sofiayan0523/sofia --limit 5

# Spot-check key URLs:
curl -sI https://sofiayan0523.github.io/sofia/                   # 200
curl -sI https://sofiayan0523.github.io/sofia/blog/              # 200
curl -sI https://sofiayan0523.github.io/sofia/blog/2024-rome/    # 200
curl -sI https://sofiayan0523.github.io/sofia/rss.xml            # 200, content-type: application/xml
```

---

## Optional follow-up tasks

These are documented in `blog-plan.md` Phase 6 and can be picked up any time:

| Item | Effort | Value |
|------|--------|-------|
| Enable giscus comments | 5 min | Reader engagement; just fill `Comments.astro` config and flip `ENABLED` |
| Add Plausible/Umami analytics | 15 min | Traffic insight; add one `<script>` to BaseLayout |
| LanguageSwitcher React Island | 30 min | zh/en toggle; i18n strings already in `src/i18n/en.ts` |
| Auto-generate per-post OG images | 1–2 hr | Better social previews; use `satori` or `@vercel/og` at build time |
| Compress remaining 421 KB florence/04 | 5 min | Marginal; quality 70 instead of 78 |

---

## Writing new posts

```bash
# Create a new MDX file
cat > src/content/posts/2026-05-new-post.mdx <<'MDX'
---
title: "新文章標題"
excerpt: "簡短描述"
category: travel        # or ai-tools | thoughts
tags: [tag1, tag2]
publishedAt: 2026-05-07
readTime: "3 min"
draft: false
coverImage: /sofia/images/posts/2026-05-new-post/cover.jpg
---

import CaptureEye from "@/components/CaptureEye.astro";

文章內容...

<CaptureEye nid="bafy..." src="/sofia/images/posts/2026-05-new-post/01.jpg" alt="..." />

更多文字...
MDX

# Drop images in public/images/posts/2026-05-new-post/
# Then:
git add . && git commit -m "post: 新文章標題" && git push
```

GitHub Actions takes ~1 minute to rebuild and redeploy.

---

## Tooling cheat sheet

| Command | Purpose |
|---------|---------|
| `npm run dev` | Local dev server with HMR |
| `npm run build` | Full production build (includes Pagefind index) |
| `npm run preview` | Serve `dist/` locally for smoke testing |
| `node scripts/download-images.mjs` | One-shot Supabase image migration (already done; idempotent) |
| `node scripts/migrate-posts.mjs` | One-shot Supabase post migration (already done; rerun to refresh from Supabase) |
| `node scripts/compress-images.mjs` | Compress JPEGs > 400 KB; idempotent |
| `node scripts/compress-images.mjs --dry-run` | Preview compression without writing |

---

## Lessons learned (worth remembering)

- **Astro 5 collection IDs include the file extension** — always strip with `.replace(/\.mdx?$/, "")` when constructing URLs
- **MDX is strict about `<` and `{`** — bare characters need escaping; safest pipeline is extract→escape→restore
- **GitHub Pages project page needs `base` set in Astro config** — and template literals using `BASE_URL` need trailing-slash normalization
- **`context.site` excludes `base`** — combine the two when generating absolute URLs (RSS, OG, canonical)
- **React Islands are paid only when used** — having `@astrojs/react` installed adds nothing to bundle; only pages that include `client:load` components ship the React runtime
- **Pagefind plays well with content collections** — wrap the post body with `<article data-pagefind-body>` to make search index article-only
